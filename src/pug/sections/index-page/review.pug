include ../../mixins/section-title

section.review.slider-section
  .container
    +section-title('Отзывы', true)

    .main-slider.main-slider--review
      .swiper-wrapper
        each item in [1,2,3,4]
          .swiper-slide
            img(src=`./assets/img/review_${item}.png` alt="")

